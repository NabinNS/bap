{
  "name": "bap",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "web": "pnpm --filter @bap/web dev",
    "api": "pnpm --filter @bap/api dev",
    "setup": "pnpm install && cd apps/api && composer install"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
